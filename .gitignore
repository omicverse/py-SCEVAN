.venv/
__pycache__/
*.pyc
dist/
build/
*.egg-info/
.pytest_cache/

# Benchmark data artifacts: keep the SMALL per-patient files (metrics.csv,
# norm_cell.txt, *_tum_cells.txt, *_runinfo.txt, *_classDf.tsv, summary.csv,
# SUMMARY.md, *.png) so the benchmark is traceable + self-documenting; exclude
# only the large derived CNA matrices and scratch (regenerate via scripts/).
benchmarks/**/*.tsv.gz
benchmarks/**/scevan_scratch/
benchmarks/**/run.log
